# #!/bin/bash

# # Update package list to ensure we install the latest versions
# apt-get update

# # Install required compilers
# apt-get install -y default-jdk   # Java Development Kit (JDK)
# apt-get install -y gcc           # GNU C Compiler (GCC)
# apt-get install -y g++           # GNU C++ Compiler (G++)
#!/bin/bash
echo "All dependencies are installed inside the Docker container!"
